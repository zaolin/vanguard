FROM ubuntu:25.04

RUN apt-get update && apt-get install -y --no-install-recommends \
    golang \
    qemu-system-x86 \
    swtpm \
    swtpm-tools \
    cryptsetup \
    lvm2 \
    busybox \
    gcc \
    make \
    mtools \
    e2tools \
    ca-certificates \
    curl \
    git \
    efitools \
    sbsigntool \
    ovmf \
    fwupd \
    systemd \
    systemd-ukify \
    systemd-boot-efi \
    && rm -rf /var/lib/apt/lists/*

# Verify key tools
RUN go version && \
    qemu-system-x86_64 --version | head -1 && \
    swtpm --version | head -1 && \
    cryptsetup --version | head -1 && \
    fwupdmgr --version | head -1 && \
    ukify --version | head -1 && \
    /usr/lib/systemd/systemd-pcrlock --version | head -1

WORKDIR /workspace
